import * as React from "react"
import { RouteComponentProps } from "react-router"

/**
 * SignupContainer UI component
 *
 * @export
 * @class SignupContainer
 * @extends {React.Component<RouteComponentProps<any>, void>}
 */

export class SignupContainer extends React.Component<RouteComponentProps<any>, void> {
  /** render */
  // tslint:disable-next-line:prefer-function-over-method
  public render() {
    return ""
  }
}

export default (
  SignupContainer as any as React.StatelessComponent<RouteComponentProps<any>>
)