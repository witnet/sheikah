describe('Create wallet', () => {
  it('should create a wallet', async () => {
    // const button = await app.client.getSelectedText('#btn')
    // console.log('app.client', Object.keys(app.client))
    // console.log('button', button)
    expect(true).toBe(true)
  })
})
